{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node app",
    "cluster": "node cluster",
    "lint": "cross-env standard",
    "lint:fix": "cross-env standard --fix"
  },
  "dependencies": {
    "config": "^2.0.1",
    "jm-server": "^2.0.8"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "lerna": "^2.11.0",
    "standard": "^12.0.1"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
