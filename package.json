{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node app",
    "cluster": "node cluster"
  },
  "dependencies": {
    "config": "^2.0.1",
    "jm-server": "^2.0.8"
  },
  "devDependencies": {
    "lerna": "^2.11.0",
    "standard": "^12.0.1"
  }
}
