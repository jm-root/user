{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node app",
    "cluster": "node cluster",
    "lint": "standard",
    "lint:fix": "standard --fix"
  },
  "lint-staged": {
    "**/*.js": [
      "npm run lint:fix",
      "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "dependencies": {
    "config": "^2.0.1",
    "jm-server": "^2.0.8"
  },
  "devDependencies": {
    "husky": "^1.2.0",
    "lerna": "^2.11.0",
    "lint-staged": "^8.1.0",
    "standard": "^12.0.1"
  }
}
